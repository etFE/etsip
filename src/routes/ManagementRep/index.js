import React, { Component } from 'react';
// import { connect } from 'dva'

// @connect(({}) => ({

// }))
export default class Slog extends Component {
    render () {
        return (
            <div>Rep</div>
        )
    }
}
